import React from "react";
import {Text, View} from 'react-native';

export const FindMonthPage: React.FC = () => {
    return (
        <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Find Month Page</Text>
    </View>
    );
};