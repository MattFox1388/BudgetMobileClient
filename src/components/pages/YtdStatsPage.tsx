import React from "react";
import {Text, View} from 'react-native';

export const YtdStatsPage: React.FC = () => {
    return (
        <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>YTD Stats Page</Text>
    </View>
    );
};